# CRLF Normalisation
* text=auto eol=lf
*.svg text -diff
*.png binary -text -eol
*.jpg binary -text -eol
*.jpeg binary -text -eol
*.gif binary -text -eol
*.mp4 binary -text -eol

# Ignore meta files in ZIP export
.gitattributes export-ignore
.gitignore export-ignore

# Never merge lockfiles
package-lock.json -diff
yarn.lock -diff
